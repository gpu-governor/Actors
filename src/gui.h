/// A simple header only gui library for basics widgets used in the engine, no need to compile just include and specify path
/// widgets availabe : Text, Buttons
#include<SDL2/SDL.h>
// Global VAR
const char font = "fonts/FreeMono.ttf";